CREATE TABLE [dbo].[ITEM_CATEGORY_TABLE] (
    [CATEGORY_ID]   INT           NOT NULL,
    [CATEGORY_NAME] VARCHAR (255) NULL, 
    CONSTRAINT [PK_ITEM_CATEGORY_TABLE] PRIMARY KEY ([CATEGORY_ID])
);

